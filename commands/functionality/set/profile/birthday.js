const { ChatInputCommandInteraction } = require("discord.js")

module.exports = {
  subCommand: "set profile birthday",
  /**
   *
   * @param {ChatInputCommandInteraction} interaction
   */
  async execute(interaction) {},
}
